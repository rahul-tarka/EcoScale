apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecoscale.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecoscale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecoscale.selectorLabels" . | nindent 4 }}
