{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ecoscale.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ecoscale.labels" . | nindent 4 }}
{{- end }}
